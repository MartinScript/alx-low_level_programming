#include "main.h"
/**
 * _abs - prints absolute of a number
 * @n: checks for function input
 * Return: return always 0 (success)
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-(n));
	}
	else
	{
		return (n);
	}
}
